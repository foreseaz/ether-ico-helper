This folder currently contains only the sender analysis of several riches participants in BAT ico.

The file is in JSON format: {"address": [sender list]}.

Sender list is a JSON array with each element representing a sender information in JSON format.

If the sender list for certain address is empty, probably the certain address is a miner or exchange address.
